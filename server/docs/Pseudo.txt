// newEmployee(fname, lname, birthdate, user_name, password, company)
//      create ID
//  	create new employee (model)
//      add to company

// newCompany(name, location, user_name, password)
//      create new company (model)
//      empty employee array

// newTrip(Date, destination, budget, discription)
//      create ID
//      create new trip (model)
//      empty expences array
//      add to company

// newExpences(price, location, discription, date)
//      create ID
//      create new Expence (mode)
//      add to Trip
//      add responsible Employee
